#include <stdio.h> // Standard input output

int main (void){
    char strName[] = "Héctor"; // Turns out, there aren't such things as string in C
    printf("Hello %s\n", strName); // Printing a variable with printf
}